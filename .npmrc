# Ensure the lockfile version and format stay consistent
lockfile-version=9.0

# Prevents the Agent from changing the "settings" block you see in the lockfile
auto-install-peers=true
exclude-links-from-lockfile=false

# Recommended for Monorepos to avoid "phantom dependencies" 
# and ensure the Agent doesn't try to use a different layout
hoist=true